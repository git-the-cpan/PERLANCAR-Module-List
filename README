DESCRIPTION

    This module is a fork of Module::List 0.003. It's exactly like
    Module::List, except that it strips the usage of Exporter, IO::Dir,
    Carp, File::Spec, with the goal of saving a few milliseconds (a casual
    test on my PC results in 11ms vs 39ms).

SEE ALSO

    Module::List

